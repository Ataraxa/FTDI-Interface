#ifndef LOGGERS_UTILS_H
#define LOGGERS_UTILS_H

#include "../include/fftw3.h"

// struct FftLogEntry {
//     uint64_t start_sample_index;  // absolute position of first sample in FFT window
//     double   input[N];            // FFT input (real part only)
//     fftw_complex output[N];       // FFT output
// };

#endif